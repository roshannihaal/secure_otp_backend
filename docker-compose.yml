version: '3.8'

services:
  api:
    container_name: secure_otp-api
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    environment:
      - NODE_ENV=development
      - PORT=${PORT}
    ports:
      - ${PORT}:${PORT}